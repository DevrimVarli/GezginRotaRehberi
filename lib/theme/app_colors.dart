import 'dart:ui';

import 'package:flutter/material.dart';

class AppColors{
  // Ana renkler
  static const primaryColor = Color(0xFF6200EE);
  static const secondaryColor = Color(0xFF03DAC6);
  static const accentColor = Color(0xFF018786);

  // Metin renkleri
  static const textPrimary = Color(0xFF000000);
  static const textSecondary = Color(0xFF757575);
  static const textTertiary = Colors.blue;

  // Arka plan renkleri
  static const backgroundLight = Color(0xFFFFFFFF);
  static const backgroundDark = Color(0xFF121212);

  // Buton renkleri
  static const buttonPrimary = primaryColor;
  static const buttonSecondary = Color(0xFF3700B3);

  // Hata ve uyarı renkleri
  static const error = Color(0xFFB00020);
  static const success = Color(0xFF4CAF50);
}